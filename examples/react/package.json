{
  "private": true,
  "dependencies": {
    "classnames": "^2.1.5",
    "director": "^1.2.0",
    "react": "^0.13.3",
    "todomvc-app-css": "^2.0.0",
    "todomvc-common": "^1.0.1"
  },
  "scripts": {
    "test": "yarn && http-server & wait-on http://localhost:8080 && cypress run",
    "cypress": "yarn && http-server & wait-on http://localhost:8080 && cypress open"
  },
  "devDependencies": {
    "cypress": "^1.4.2",
    "http-server": "^0.11.1",
    "wait-on": "^2.1.0"
  }
}
